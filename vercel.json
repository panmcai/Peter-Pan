{
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install && npx next build",
  "devCommand": "npx next dev",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["hkg1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
